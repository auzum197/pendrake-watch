[package]
name = "pendrake-watch"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "pendrake_watch_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Thin IPC client over the daemon's Unix socket. This crate never links the
# zingolib engine. The GUI is a client, the daemon owns the wallet file.
tokio = { version = "1", features = ["net", "io-util", "time"] }
dirs = "5"
tauri-plugin-deep-link = "2.4.9"

[target.'cfg(any(target_os = "macos", windows, target_os = "linux"))'.dependencies]
# `deep-link` feature focuses the running window when a pendrake:// URL opens.
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }

